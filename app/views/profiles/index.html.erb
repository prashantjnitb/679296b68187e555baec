<h1> Users on App</h1>
<% if @users.present? %>
  <table>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.email %></td>
          <% if user.followers.where(id: current_user.id).present? %>
          	<td><%= link_to "Unfollow", unfollow_profile_path(user.id), method: :delete %></td>
          <% else %>
            <td><%= link_to "Follow", follow_profile_path(user.id), method: :post %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  No user have joined the network yet. You will see other users once they join the network.
<% end %>